# Enterprise GRC Tool Frontend

React and Vite frontend for the Enterprise GRC Tool.

## Scripts

- `npm run dev`: start the local Vite development server
- `npm run build`: type-check and build the production bundle
- `npm run start`: serve the built frontend on the Railway-provided port

## Deployment

This frontend is configured for Railway deployment from the `frontend` directory using the checked-in Railway config files.
